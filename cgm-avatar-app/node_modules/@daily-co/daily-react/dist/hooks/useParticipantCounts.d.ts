import { DailyEventObject, DailyParticipantCounts } from '@daily-co/daily-js';
interface Props {
    onParticipantCountsUpdated?(ev: DailyEventObject<'participant-counts-updated'>): void;
}
/**
 * Returns participant counts for hidden and present participants.
 */
export declare const useParticipantCounts: ({ onParticipantCountsUpdated, }?: Props) => DailyParticipantCounts;
export {};
