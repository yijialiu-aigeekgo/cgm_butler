interface Countdown {
    hours: number;
    minutes: number;
    seconds: number;
}
interface Props {
    onCountdown?(countdown: Countdown): void;
}
export declare const useRoomExp: ({ onCountdown }?: Props) => {
    ejectDate: Date | null;
};
export {};
