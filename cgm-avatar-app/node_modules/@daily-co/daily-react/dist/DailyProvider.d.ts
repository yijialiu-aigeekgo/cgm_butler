import { DailyCall, DailyFactoryOptions } from '@daily-co/daily-js';
import { Provider } from 'jotai';
import React from 'react';
type BaseProps = DailyFactoryOptions | {
    callObject: DailyCall | null;
};
type Props = BaseProps & {
    jotaiStore?: React.ComponentProps<typeof Provider>['store'];
};
export declare const DailyProvider: React.FC<React.PropsWithChildren<Props>>;
export {};
